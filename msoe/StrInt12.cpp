#include <iostream>
#include <vector>
#include <string>
#include <math.h>
using namespace std;
/* int main() {
    cout << "Enter a string";
    int len = 0;
    string s;
    cin >> s;
    int currentWordNum = 0;
    for (lcv = 0; lcv < s.length(); lcv++) {

    }
    char words[20][10];
    char currentWord[20];
    int listLength = 0;
    for (int lcv = 0; lcv < s.length(); lcv++) {
        if (s[lcv] == " ") {
            words[currentWordNum] = currentWord;
            currentWord = ""
            currentWordNum++;
        } else {
            currentWord = currentWord + s[lcv];
        }
    }
    for ()
}
*/
