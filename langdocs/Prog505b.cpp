#include <iostream>
#include <fstream>
#include <string>
using namespace std;

int main() {
    string first, last;
    int s1, s2, s3, s4, s5, aS, bS, cS, dS, fS;
    ifstream f("prog505b.dat");
    if (f.is_open()) {
        while (f >> first >> last >> s1 >> s2 >> s3 >> s4 >> s5) {
            
        }
    }
}